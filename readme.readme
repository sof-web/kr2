Объяснение работы алгоритма
1. Структура Min-Heap

    Бинарная минимальная куча — это полное бинарное дерево, где значение в каждом узле меньше или равно значениям в его дочерних узлах.

    Реализуется через массив, где для узла с индексом i:

        Родитель: (i-1)/2

        Левый потомок: 2*i + 1

        Правый потомок: 2*i + 2

2. Основные операции
Вставка (insert)

    Элемент добавляется в конец массива.

    Выполняется просеивание вверх (heapifyUp):

        Пока элемент меньше родителя, он меняется с родителем местами.

        Поднимается до корня или пока свойство кучи не восстановится.

Извлечение минимума (extractMin)

    Минимум всегда находится в корне (индекс 0).

    Последний элемент массива перемещается в корень.

    Выполняется просеивание вниз (heapifyDown):

        Сравнивается с дочерними элементами.

        Меняется с наименьшим из них.

        Процесс повторяется, пока свойство кучи не восстановится.



Временная сложность-Операция-Сложность	Объяснение
insert-	O(log n)	-Просеивание вверх проходит по высоте дерева, которая равна log₂(n).
extractMin-	O(log n)-	Просеивание вниз также проходит по высоте дерева log₂(n).
display	-O(n)-	Требуется пройти по всем элементам массива.
isEmpty	-O(1)	-Проверка размера вектора — константная операция.

Почему log n?
Высота полного бинарного дерева с n узлами равна ⌊log₂ n⌋. Просеивание выполняется за время, пропорциональное высоте, поэтому O(log n).

